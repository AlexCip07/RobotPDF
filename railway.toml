[build]
builder = "nixpacks"
buildCommand = "npm cache clean --force && npm ci --no-optional && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"

[deploy.railway]
DATABASE_URL = "${RAILWAY_POSTGRESQL_CONNECTION_URL}" 